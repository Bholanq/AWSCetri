AWS Lambda Layers are a way to **share reusable code, libraries, or dependencies** across multiple Lambda functions.

Instead of packaging the same dependencies inside every Lambda deployment package, you place them in a **Layer** and attach the layer to functions.

![[Pasted image 20260517071203.png]]
